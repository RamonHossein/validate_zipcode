require 'spec_helper'

describe ValidateZipcode do
  it 'has a version number' do
    expect(ValidateZipcode::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
